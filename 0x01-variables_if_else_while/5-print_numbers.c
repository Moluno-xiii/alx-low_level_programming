#include <stdio.h>

/**
 * main - Prints single digit numbers of base 10 starting with 0
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char a;

	for (a =0; a < 10; a++)
	{
		printf("%d", a);
	}
	printf("\n");

	return 0;
}
