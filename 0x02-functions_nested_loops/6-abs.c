#include "main.h"
/**
 * _abs -Pprints absolute value of a number.
 * @num: number to be printed
 * Return: absolute value of number.
 */

int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}else
	{
		return (-num);
	}

	return (num);
}
